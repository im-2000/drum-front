export const selectKick = (reduxState) => reduxState.looperReducer.kick;
export const selectSnare = (reduxState) => reduxState.looperReducer.snare;
export const selectClap = (reduxState) => reduxState.looperReducer.clap;
export const selectTom = (reduxState) => reduxState.looperReducer.tom;
export const selectCh = (reduxState) => reduxState.looperReducer.ch;
export const selectOh = (reduxState) => reduxState.looperReducer.oh;
export const selectCrash = (reduxState) => reduxState.looperReducer.crash;
