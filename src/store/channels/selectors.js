export const selectDrums = (reduxState) => reduxState.channelsReducer.drums;
export const selectBass = (reduxState) => reduxState.channelsReducer.bass;
export const selectMelody = (reduxState) => reduxState.channelsReducer.melody;
export const selectPad = (reduxState) => reduxState.channelsReducer.pad;
